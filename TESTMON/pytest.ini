[pytest]
testpaths = tests
pythonpath = src
addopts = -ra --strict-markers
markers =
    integration: integration tests spanning multiple modules
    slow: tests that take longer to execute
    fragile: tests that fail when production code is intentionally modified
